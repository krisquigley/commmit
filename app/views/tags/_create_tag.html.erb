<%= form_with(model: [story, Tag.new], html: { class: 'form-inline' }) do |f| %>
  <%= f.label :name, t('tags.form.name.label'), class: 'sr-only' %>
  <div class="input-group mb-2 mr-sm-2">
    <%= f.text_field :name, class: 'form-control', placeholder: t('tags.form.name.label') %>
    <div class="input-group-append">
      <%= f.submit t('tags.form.create'), class: 'btn btn-success mb-2' %>
    </div>
  </div>
<% end %>
