<%= form_for @user do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true %>
  <%= f.label :team %>
  <%= f.collection_select(:team_id, Team.all, :id, :name, {prompt: true, required: true}) %>
  <%= f.submit %>
<% end %>