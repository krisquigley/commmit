<% if object.errors.any? %>
  <div class="alert alert-danger">
    <h2><%= pluralize(object.errors.count, "error") %> <%= t('form.errors') %>:</h2>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>