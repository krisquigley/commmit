<small data-controller="relative-time" class="align-bottom">
  <i>
    <%= completed_or_added(story) %> 
    <span data-relative-time-target="relativeTime" title="<%= story.completed_at || story.created_at %>"><%= story.completed_at || story.created_at %></span>
  </i>
</small>