<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" data-turbo="false">
  <%= link_to current_commmits_path, class: "navbar-brand" do %>
    <%= render 'shared/icons/logo_icon', classes: '' %>
  <% end %>

  <div class="section-heading">
    <div>
      <% if content_for :productivity %>
        <%= render 'shared/productivity_badge', productivity: content_for(:productivity) %> 
      <% end %>
    </div>
    <div style="overflow: hidden">
      <h6 class="ml-2" title="<%= content_for :nav_header %>" data-toggle="sectionHeadingTooltip" data-placement="bottom" data-action="click->navigate#showSectionHeadingTooltip">
        <%= content_for :nav_header %>
      </h6>
    </div>
  </div>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <div class="dropdown-divider"></div>
        <li class="nav-item">
          <%= link_to t('commmits.index.header'), commmits_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to t('values.index.header'), values_path, class: "nav-link" %>
        </li>
        <div class="dropdown-divider"></div>
        <li class="nav-item">
          <%= link_to t('feedback'), 'https://forms.gle/pEE4SxD9JE2KJRU2A', target: '_about', class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= button_to t('sessions.logout'), 
                        logout_path, 
                        form_class: "nav-link", 
                        class: "btn nav-link w-100 text-right", 
                        method: :delete, 
                        data: { turbo: false } %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>