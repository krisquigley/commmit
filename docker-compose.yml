version: '3'

networks:
  commmit:

services:
  postgres:
    container_name: commmit-postgres
    image: postgres:10-alpine
    ports:
      - '9876:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - commmit

  redis:
    container_name: commmit-redis
    image: redis:alpine
    volumes:
      - redisdata:/data
    ports:
      - '6789:6379'
    networks:
      - commmit

volumes:
  pgdata:
  redisdata:
