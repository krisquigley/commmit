# It should show the dashboard if logged in on their domain

# It should redirect to the login page if not logged in on their domain

# It should redirect the user to their domain if the account doesn't belong to them
